{
  "name": "screen-stream-site",
  "version": "1.0.0",
  "private": true,
  "description": "WebRTC screen stream site with a Node WebSocket signaling server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check viewer.js && node --check sender.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
